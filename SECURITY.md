# 安全策略

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| 0.1.x | ✅ 维护中 |
| < 0.1.0 | ❌ 不受支持 |

## 报告漏洞

**请勿**为安全问题开公开 Issue。请使用 GitHub 的私有漏洞报告功能：

1. 打开仓库的 **Security → Report a vulnerability**
2. 按模板填写：影响的版本、漏洞描述、复现步骤（尽量精简）、期望与实际行为
3. 我们会尽快响应（目标 72 小时内确认），并在修复后与你同步

## 处理流程

1. 确认漏洞并评估影响范围
2. 在私有分支修复并补充回归测试
3. 发布修复版本（patch 版本号递增），公开披露时给予合理的协调期

## 已知安全边界

本插件为**本机单用户工具**（`dsh web` 默认绑定 127.0.0.1），聚合数据不出本机。
若你把它暴露到公网或多人共享环境，请自行评估以下风险：

- `/api/usageStats/*` 端点无鉴权，会返回本机全部会话的 token 用量与估算花费
- 会话日志目录（`$DSH_HOME/sessions`）包含模型对话内容，请勿共享
